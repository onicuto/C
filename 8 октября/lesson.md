#include <stdio.h>

int main() {
    int n;                
    int t;                
    int target = 25;     
    int count = 0;        
    int was = 0;         

    printf("Введите количество дней сентября: ");
    scanf("%d", &n);

    printf("Введите температуры по дням:\n");

    for (int i = 1; i <= n; i++) {
        scanf("%d", &t);
        if (t == target) {
            was = 1;
            count++;
        }
    }

    if (was)
        printf("Температура +%d была %d дней(дня).\n", target, count);
    else
        printf("Температуры +%d не было.\n", target);

    return 0;
}


⸻

🔹 Пример 2. Поиск максимума и минимума (без массива)

#include <stdio.h>

int main() {
    int n, temp;
    int max, min;

    printf("Введите количество дней: ");
    scanf("%d", &n);

    if (n <= 0) {
        printf("Неверное количество.\n");
        return 0;
    }

    printf("Введите температуры:\n");
    scanf("%d", &temp);
    max = min = temp;

    for (int i = 2; i <= n; i++) {
        scanf("%d", &temp);
        if (temp > max) max = temp;
        if (temp < min) min = temp;
    }

    printf("Максимальная температура: %d\n", max);
    printf("Минимальная температура: %d\n", min);

    return 0;
}


⸻

🔹 Пример 3. Проверка упорядоченности данных (возрастание)

#include <stdio.h>

int main() {
    int n;
    int prev, curr;
    int ordered = 1; // 1 — пока считаем, что последовательность упорядочена

    printf("Введите количество чисел: ");
    scanf("%d", &n);

    if (n < 2) {
        printf("Мало данных для проверки.\n");
        return 0;
    }

    printf("Введите первое число: ");
    scanf("%d", &prev);

    for (int i = 2; i <= n; i++) {
        scanf("%d", &curr);
        if (curr < prev)
            ordered = 0;
        prev = curr;
    }

    if (ordered)
        printf("Последовательность упорядочена по возрастанию.\n");
    else
        printf("Последовательность неупорядочена.\n");

    return 0;
}



